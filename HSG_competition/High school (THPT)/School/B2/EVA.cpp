#include <bits/stdc++.h>
using namespace std;

fstream fi(".\\txt\\EVA.INP");

int n, k, m;

int main()
{
    ios::sync_with_stdio(false);
    cin.tie(0);
    cout.tie(0);

    fi >> n >> m;

    return 0;
}