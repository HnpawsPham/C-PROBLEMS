#include <bits/stdc++.h>
using namespace std;
#define el "\n"

int n;
const int maxn = 16;
int a[maxn][maxn];
int mincost = INT_MAX;

void solve(){
    

    return;
}

int main(){
    ios::sync_with_stdio(0);
    cin.tie(0);
    cout.tie(0);

    cin>>n;
    for(int i = 0; i<n;i++){
        for(int j=0;j<n;j++) cin>>a[i][j];
    }
    
    return 0;
}